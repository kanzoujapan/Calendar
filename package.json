{
  "name": "calendar-fullstack",
  "private": true,
  "scripts": {
    "dev": "npm-run-all -p dev:*",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "sh -c \"cd backend && python -m flask --app app run -p 3000 --debug\"",
    "build": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}